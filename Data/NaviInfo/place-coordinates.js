/* Verified coordinates for NaviInfo place cards.
 * Keys are generated from the card heading by maps.js.
 * Add only coordinates verified for the named place.
 */
window.YOLA_NAVIGATION_COORDINATES = window.YOLA_NAVIGATION_COORDINATES || Object.freeze({
});
